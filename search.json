[{"title":"网页渲染修改日志","path":"/2026/05/20/blog-theme/","content":"##2026.5.20#取消“红线竖”里面包含 #、、| 的标题效果_config.yml header_prefix: h2: ‘#’ h3: ‘’ h4: ‘|’ h5: ‘:’改为_config_stellar.yml header_prefix: h2: ‘ ‘ h3: ‘ ‘ h4: ‘ ‘ h5: ‘ ‘ #更明显的标题变化_custom.styl$fsp0 ‘calc(%s - 0px)’ % $fs-body$fsp1 ‘calc(%s - 1px)’ % $fs-body$fsp2 ‘calc(%s - 2px)’ % $fs-body$fsp3 ‘calc(%s - 3px)’ % $fs-body 改为$fsh1 ‘calc(%s + 14px)’ % $fs-body$fsh2 ‘calc(%s + 11px)’ % $fs-body$fsh3 ‘calc(%s + 6px)’ % $fs-body$fsh4 ‘calc(%s + 2px)’ % $fs-body$fsh5 ‘calc(%s + 1px)’ % $fs-body","tags":["blog"],"categories":["Blogs"]},{"title":"Hello World","path":"/2026/05/14/hello-world/","content":"Quick StartCreate a new post$ hexo new My New Post Run server$ hexo server Generate static files$ hexo generate Deploy to remote sites$ hexo deploy"},{"title":"极小离线渲染实现","path":"/2026/05/14/first-renderer/","content":"极小离线渲染实现我的第一篇博文。记录一下我写出的一个极小的 cpp 渲染器。 基本对象的定义参考了《Physically Based Rendering: From Theory to Implementation 3rd》。 目前实现了简单的光照模型，为了使得效果明显，该图使用 lambert diffuse + ambient(float)。 渲染效果如下： 基本对象定义首先按照 pbrt 的思想定义基本对象，目前仅需要： vec（vector） dot（point） ray 的三维坐标系即可。 vec3 与 dot3 的实现可以有更简洁的构造函数，但这样更明了，更数学，不是吗。 vec3public: T x, y, z; // 3D vector components (x,y,z) // constructors /** @brief default constructor (0,0,0) */ vec3() this-x = T(0); this-y = T(0); this-z = T(0); /** @brief parameterized constructor */ vec3(T x_val, T y_val, T z_val) this-x = x_val; this-y = y_val; this-z = z_val; /** * @brief vec[0] = x, vec[1] = y, vec[2] = z */ T operator [] (int i) const switch (i) case 0: return this-x; case 1: return this-y; case 2: return this-z; default: return T(0); /** * @brief calculate the length of the vector */ float length() const return std::sqrt(this-x * this-x + this-y * this-y + this-z * this-z); float length_squared() const return this-x * this-x + this-y * this-y + this-z * this-z; /** * @brief normalize */ vec3 normalize() const float len = this-length(); if (len 0) return vec3(this-x / len, this-y / len, this-z / len); else return vec3(0, 0, 0); // return zero vector if length is zero to avoid division by zero // other operations T min_component() const return std::min(this-x, std::min(this-y, this-z)); T max_component() const return std::max(this-x, std::max(this-y, this-z)); T max_dimension() const if (this-x this-y this-x this-z) return 0; // x is largest else if (this-y this-z) return 1; // y is largest else return 2; // z is largest friend vec3 min(vec3T v1, vec3T v2) return vec3( std::min(v1.x, v2.x), std::min(v1.y, v2.y), std::min(v1.z, v2.z) ); friend vec3 max(vec3T v1, vec3T v2) return vec3( std::max(v1.x, v2.x), std::max(v1.y, v2.y), std::max(v1.z, v2.z) ); dot3类的实现 public: T x, y, z; // 3D coordinates (x,y,z) // constructors /** @brief default constructor (0,0,0) */ dot3() this-x = T(0); this-y = T(0); this-z = T(0); /** @brief parameterized constructor */ dot3(T x_val, T y_val, T z_val) this-x = x_val; this-y = y_val; this-z = z_val; /** @brief dot[0] = x, dot[1] = y, dot[2] = z */ T operator[](int i) const switch(i) case 0: return this-x; case 1: return this-y; case 2: return this-z; default: return T(0); /** * @brief form dot3 to vec3 * */ vec3T to_vec3() const return vec3T(this-x, this-y, this-z); /** * @brief constructor between dot3f and dot3i, explicit to prevent implicit conversion * */ template typename T2 explicit dot3(dot3T2 d) this-x = static_castT(d.x); this-y = static_castT(d.y); this-z = static_castT(d.z); 运算符重载写在vec3 dot3类定义后,避免了部分运算符重载函数交叉依赖导致的错误template typename Tinline dot3T operator+(const dot3T p, const vec3T v) return dot3T(p.x + v.x, p.y + v.y, p.z + v.z);template typename Tinline dot3T operator-(const dot3T p, const vec3T v) return dot3T(p.x - v.x, p.y - v.y, p.z - v.z);template typename Tinline vec3T operator-(const dot3T p, const dot3T v) return vec3T(p.x - v.x, p.y - v.y, p.z - v.z);// --- vec3 ---template typename Tinline vec3T operator+(const vec3T v1, const vec3T v2) return vec3T(v1.x + v2.x, v1.y + v2.y, v1.z + v2.z);template typename Tinline vec3T operator-(const vec3T v1, const vec3T v2) return vec3T(v1.x - v2.x, v1.y - v2.y, v1.z - v2.z);template typename Tinline T operator*(const vec3T v1, const vec3T v2) // Dot Product return v1.x * v2.x + v1.y * v2.y + v1.z * v2.z;template typename Tinline vec3T operator*(const vec3T v, T scalar) return vec3T(v.x * scalar, v.y * scalar, v.z * scalar);template typename Tinline vec3T operator*(T scalar, const vec3T v) // support scalar * vec return v * scalar;template typename Tinline vec3T operator/(const vec3T v, T scalar) float inv = 1.0f / scalar; return v * inv;template typename Tinline vec3T operator-(const vec3T v) // Unary negation return vec3T(-v.x, -v.y, -v.z);template typename Tinline vec3T operator+=(vec3T v1, const vec3T v2) v1.x += v2.x; v1.y += v2.y; v1.z += v2.z; return v1;template typename Tinline vec3T operator-=(vec3T v1, const vec3T v2) v1.x -= v2.x; v1.y -= v2.y; v1.z -= v2.z; return v1;template typename Tinline vec3T operator*=(vec3T v, T scalar) v.x *= scalar; v.y *= scalar; v.z *= scalar; return v;template typename Tinline vec3T operator/=(vec3T v, T scalar) float inv = 1.0f / scalar; v.x *= inv; v.y *= inv; v.z *= inv; return v;template typename Tinline bool operator==(const vec3T v1, const vec3T v2) return v1.x == v2.x v1.y == v2.y v1.z == v2.z;template typename Tinline vec3T operator^(const vec3T v1, const vec3T v2) // Cross Product return vec3T( v1.y * v2.z - v1.z * v2.y, v1.z * v2.x - v1.x * v2.z, v1.x * v2.y - v1.y * v2.x ); 常用类型的声明using dot3f = dot3float;using dot3i = dot3int;using vec3f = vec3float;using vec3i = vec3int; 射线的定义，参考pbrt// MARK: ray definitionsclass ray public: dot3f origin; // ray origin vec3f direction; // ray direction mutable float t_max; // maximum ray extent mutable float time; // ray time const void* medium; // ray medium // constructors /** @brief default constructor (origin at (0,0,0) and direction (0,0,1)) */ ray() : origin(dot3f(0, 0, 0)), direction(vec3f(0, 0, 1)), t_max(std::numeric_limitsfloat::infinity()), time(0.0f), medium(nullptr) /** @brief parameterized constructor */ ray(dot3f origin_val, vec3f direction_val) : origin(origin_val), direction(direction_val), t_max(std::numeric_limitsfloat::infinity()), time(0.0f), medium(nullptr) dot3f operator()(float t) const return origin + direction * t; ; ball定义基本对象定义完成，接下来定义一个球由于后续需要用到ray与球求交点，在内部提前定义好对于方法方便调用class ball public: dot3f center; float radius; vec3i color; ball(dot3f center, float radius, vec3i color = vec3i(255, 255, 255)) : center(center), radius(radius), color(color) ; float hit(dot3f ray_origin, vec3f ray_direction) vec3f center_to_origin = ray_origin - center; float a = ray_direction * ray_direction; float b = 2.0f * (center_to_origin * ray_direction); float c = center_to_origin * center_to_origin - radius * radius; float delta = b * b - 4 * a * c; if (delta 0) return -1.0f; // No intersection float sqrt_delta = std::sqrt(delta); float t1 = (-b - sqrt_delta) / (2 * a); float t2 = (-b + sqrt_delta) / (2 * a); return t1 0 ? t1 : t2; // Return the closest positive intersection ;#endif // BALL_H 主函数接下来是主函数的编写，此时需要规定使用何种坐标系我们采用右手坐标系（即z轴指向我们）使用stb_image_write.h 来输出png定义常量#define IMAGE_SIZE 1000 // sceen size by pixels#define screen_size 5.0f // screen size in world #define env_light 0.15f // ambient 默认情况会使用stack来放我们的数据，但在图片情况下这是不够的，改用vector堆来承载。// vec3f views[IMAGE_SIZE][IMAGE_SIZE]; // it cant bigger than 1MB? by stackstd::vectorstd::vectorvec3f views(IMAGE_SIZE, std::vectorvec3f(IMAGE_SIZE)); 小孔相机模型，视点，画布， 物体 // base definitions dot3f eye(0, 0, 1); ball firball(dot3f(0, 0, -7), 5.0f); 直射光 vec3f light_dir(1, 1, -1); light_dir = light_dir.normalize(); 对每一个像素进行检测，使用了简单的rasterization步骤dot3f(i*screen_size/(IMAGE_SIZE) - screen_size/2.0f, j*screen_size/(IMAGE_SIZE) - screen_size/2.0f, 0.0f) 球的法向使用交点与球心的向量即可。 for(int i = 0; i IMAGE_SIZE; i++) for(int j = 0; j IMAGE_SIZE; j++) dot3f ray_origin = eye; vec3f ray_direction = (dot3f(i*screen_size/(IMAGE_SIZE) - screen_size/2.0f, j*screen_size/(IMAGE_SIZE) - screen_size/2.0f, 0.0f) - eye).normalize(); float t = firball.hit(ray_origin, ray_direction); if (t 0) dot3f hit_point = ray_origin + t * ray_direction; vec3f firball_normal = (hit_point - firball.center).normalize(); // -light_dir /** * @brief half-lambert shading, the diffuse term is (N dot L + 1) / 2 * */ float diffuse = std::max(0.0f, firball_normal * (-light_dir) + env_light); // float diffuse = std::max(0.0f, firball_normal * (-light_dir)/2.0f + 0.5f + env_light); // simple diffuse shading views[i][j] = vec3f(255 * diffuse, 255 * diffuse, 255 * diffuse); else views[i][j] = vec3f(0, 0, 0); 将数据装入png希望的数据容器 // png rendering unsigned char* pixels = new unsigned char[IMAGE_SIZE * IMAGE_SIZE * 3]; for(int j = 0; j IMAGE_SIZE; j++) for(int i = 0; i IMAGE_SIZE; i++) int pixel_index = (j * IMAGE_SIZE + i) * 3; // 0-255 pixels[pixel_index + 0] = (unsigned char)std::min(255.0f, std::max(0.0f, views[i][j].x)); pixels[pixel_index + 1] = (unsigned char)std::min(255.0f, std::max(0.0f, views[i][j].y)); pixels[pixel_index + 2] = (unsigned char)std::min(255.0f, std::max(0.0f, views[i][j].z)); 最后调用库进行输出 stbi_write_png(output.png, IMAGE_SIZE, IMAGE_SIZE, 3, pixels, IMAGE_SIZE * 3))","tags":["Renderer","Graphics"],"categories":["Graphics"]}]